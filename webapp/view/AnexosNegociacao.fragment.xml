<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout" xmlns:form="sap.ui.layout.form">

    <Dialog
        title="Anexos Negociação"
        contentWidth="400px"
        contentHeight="600px"
        >
    
        <subHeader>
        </subHeader>
    
        <beginButton>
        </beginButton>
    
        <endButton>
            <Button
                text="Fechar"
                press="onFechar"
                />
        </endButton>
    
        <content>

            <UploadCollection
                uploadUrl="/sap/ZNEGOCIACAO_SRV/NegociacaoSet('0000000185')/anexos/"
                id="UploadCollection"
                maximumFilenameLength="55"
                maximumFileSize="10"
                multiple="false"
                change="onChange"
                fileSizeExceed="onFileSizeExceed"
                selectionChange="onSelectionChange"
                typeMissmatch="onTypeMissmatch"
                uploadComplete="onUploadComplete"
                beforeUploadStarts="onBeforeUploadStarts"
                items="{
                    path: 'anexos',
                    templateShareable: false
                }"
                mode="MultiSelect">
                <toolbar>
                    <OverflowToolbar id="myId">
                        <Title id="attachmentTitle" />
                        <ToolbarSpacer />
                        <Button
                            id="downloadButton"
                            text="Download"
                            press="onDownloadItem"
                            enabled="false"
                            type="Transparent" />
                        <Button
                            id="versionButton"
                            text="Upload a new version"
                            press="onVersion"
                            enabled="false"
                            type="Transparent" />
                        <UploadCollectionToolbarPlaceholder/>
                    </OverflowToolbar>
                </toolbar>
                <items>
                    <UploadCollectionItem
                        documentId="{documentId}"
                        fileName="{fileName}"
                        mimeType="{mimeType}"
                        thumbnailUrl="{thumbnailUrl}"
                        url="{url}"
                        enableEdit="false"
                        enableDelete="false"
                        visibleDelete="false"
                        visibleEdit="false"
                        attributes="{
                            path: 'attributes',
                            templateShareable: false
                        }"
                        statuses="{
                            path: 'statuses',
                            templateShareable: false
                        }"
                        selected="{selected}">
                        <attributes>
                            <ObjectAttribute
                                title="{title}"
                                text="{
                                    parts: [ 'text', 'type' ],
                                    formatter : '.formatAttribute'
                                }"
                                active="false" />
                        </attributes>
                        <statuses>
                            <ObjectStatus title="{title}"
                                text="{text}"
                                state="{state}" />
                        </statuses>
                    </UploadCollectionItem>
                </items>
            </UploadCollection>

        </content>
    </Dialog>

</core:FragmentDefinition>
